Generate 5 short, action-oriented Call-to-Action (CTA) phrases for a product named "{{name}}".
Objective: {{objective}}.
Description: {{description}}.
Return ONLY a JSON object with this format: { "data": ["Shop Now", "Learn More", ...] }
