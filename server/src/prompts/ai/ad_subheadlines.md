Generate 3 detailed ad descriptions (subheadlines) for a product named "{{product_name}}". 
Description: {{description}}.
Return ONLY a JSON object with this format: { "data": ["Desc 1", "Desc 2", ...] }
